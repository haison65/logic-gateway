# Logic node 2 — P0 local (go run); UDP port khác logic-1
udp:
  listen: 127.0.0.1
  port: 9101

gateway:
  host: 127.0.0.1
  port: 9000

node:
  node_id: 3
  instance_id: logic-2-dev
  ip: 127.0.0.1

services:
  - service_id: 100
    service_name: call
    message_types: [1001, 1002, 1003]

heartbeat:
  interval: 1000ms
  timeout: 3000ms
